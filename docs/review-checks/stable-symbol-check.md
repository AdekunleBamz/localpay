# Stable Symbol Check

Confirm `NEXT_PUBLIC_LOKA_STABLE_SYMBOL` matches the token shown to users in checkout.

For the default MiniPay setup, reviewers should see `USDm` and not CELO, USDC, or USDT unless the release intentionally changes the default.
