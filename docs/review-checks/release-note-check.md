# Release Note Check

Before handoff, record the app commit, deployed URL, ledger address, token defaults, and wallet used for smoke testing.

Keep the release note with the preview so later receipt reviews can trace the tested deployment.
