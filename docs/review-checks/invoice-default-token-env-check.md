# Invoice Default Token Env Check

- Review the default checkout token after changing environment values.
- Confirm the UI selects the intended token for new invoices.
- Verify shared invoices keep their original token instead of using a new default.
