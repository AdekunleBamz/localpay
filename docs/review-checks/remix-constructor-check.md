# Remix Constructor Check

When deploying the ledger from Remix, record `initialOwner_`, `treasury_`, and `feeBps_` exactly as submitted.

Reviewers need these values to verify the contract and compare the deployed configuration with the app environment.
