# Environment Sync Check

Before sharing a production preview, compare `.env.local` with the README and Remix deployment guide.

Confirm the ledger address, Celo chain ID, stable token address, stable symbol, and stable decimals all describe the same deployment.
