# Local Setup Check

Before onboarding a reviewer locally, confirm `npm install` and `npm run dev` work with the documented environment variables.

Use a test ledger address or a clearly labeled production read-only setup for local review.
