# Deployment Verification Check

After deploying from Remix, verify the ledger contract with the same constructor values used for deployment.

Keep the verification link with the release handoff so reviewers can inspect the deployed contract source.
