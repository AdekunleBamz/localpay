# Transaction Confirmation Check

After sending a payment, wait for the wallet to return a transaction hash and confirm the app receipt uses that same hash.

Capture the hash in the release note so it can be matched to the ledger event later.
