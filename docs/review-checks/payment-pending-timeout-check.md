# Payment Pending Timeout Check

Let a submitted transaction remain pending longer than the normal confirmation window.
Confirm the UI avoids duplicate payment prompts and gives an explorer recovery path.
