# Env Example Check

When a public environment variable changes, update `.env.example` before sharing setup instructions with reviewers.

Compare the example file with the README environment block so new reviewers do not copy stale ledger or token values.
