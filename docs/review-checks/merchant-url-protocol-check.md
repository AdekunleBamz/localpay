# Merchant URL Protocol Check

- Enter merchant contact URLs with supported and unsupported protocols.
- Confirm unsupported protocols are rejected or rendered safely.
- Verify accepted URLs open outside checkout without replacing payment state.
