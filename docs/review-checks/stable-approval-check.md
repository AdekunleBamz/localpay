# Stable Approval Check

For ERC-20 payments, confirm the token approval completes before the `payToken(...)` transaction is submitted.

Capture both transaction hashes when a reviewer is validating a new stable token or ledger deployment.
