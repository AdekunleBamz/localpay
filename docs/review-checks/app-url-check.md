# App URL Check

Confirm `NEXT_PUBLIC_APP_URL` matches the deployed app URL used to generate checkout links and QR codes.

If a temporary preview URL is used, keep it clearly labeled in the release note.
