# Ledger Address Check

Confirm `NEXT_PUBLIC_LOKA_LEDGER_ADDRESS` points to the deployed `LokaPayLedger` contract before creating a merchant request.

The address in the app environment should match the current Celo Mainnet ledger listed in the deployment guide.
