# API Invoice Large Amount Check

- Submit an invoice API request above the supported display range.
- Confirm the API returns a controlled validation response.
- Verify the UI does not render a malformed checkout total.
