# Fee Token Fallback Check

Unset the preferred fee token locally and confirm the fallback token is displayed clearly.
Record the environment variable state and resulting token label.
Restore production-like settings before final smoke tests.
