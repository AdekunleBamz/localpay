# Native Token Receipt Check

For a native CELO payment, confirm the receipt distinguishes the native token path from ERC-20 token addresses.

This keeps native receipts clear when downstream reports include both CELO and stablecoin payments.
