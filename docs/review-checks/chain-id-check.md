# Chain ID Check

Before release review, confirm `NEXT_PUBLIC_CELO_CHAIN_ID` is set to `42220` for Celo Mainnet.

Only use a testnet chain ID when the preview and release note are explicitly labeled as testnet.
