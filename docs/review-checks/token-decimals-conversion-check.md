# Token Decimals Conversion Check

Confirm the requested amount is converted using the decimals of the selected token before calling `payToken(...)`.

Use a small payment request so the displayed amount and submitted token amount can be compared safely.
