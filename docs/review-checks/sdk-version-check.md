# SDK Version Check

Before release review, confirm the installed `@bamzzstudio/loka-sdk` package contains the ledger ABI and token constants expected by the app.

Record the SDK tarball commit or package version in the release handoff.
