# LocalPay Invoice Share Native Fallback Check

Open the invoice share action in a browser without native share support.
Confirm LocalPay falls back to copying the checkout link.
Verify the user receives visible confirmation of the fallback action.
