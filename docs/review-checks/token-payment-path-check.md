# Token Payment Path Check

Test one ERC-20 stablecoin payment and confirm the ledger call uses `payToken(...)`.

The receipt should preserve the token address, amount, invoice ID, payer, and merchant details for reconciliation.
