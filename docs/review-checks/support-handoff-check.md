# Support Handoff Check

Before publishing a production checkout link, record where users should report failed payment attempts.

Include the checkout URL, wallet mode, selected token, invoice ID, and transaction hash when available.
