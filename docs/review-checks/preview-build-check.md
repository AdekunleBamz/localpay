# Preview Build Check

Before sharing a Vercel preview, run `npm run lint` and `npm run build` from a clean checkout.

Attach the results to the release note when ledger, token, or chain configuration changed.
