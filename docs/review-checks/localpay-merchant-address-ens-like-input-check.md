# LocalPay Merchant Address ENS Like Input Check

Enter an ENS-style name where a Celo address is required.
Confirm LocalPay rejects it unless explicit resolution support exists.
Verify the error names the expected address format.
