# Rollback Check

Before production deployment, keep the previous working ledger address and preview URL available in the release handoff.

If checkout issues appear after launch, reviewers should be able to restore the last known good environment variables quickly.
